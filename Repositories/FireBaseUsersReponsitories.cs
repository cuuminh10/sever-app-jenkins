using gmc_api.Base;
using gmc_api.Base.InterFace;
namespace gmc_api.Repositories
{
    public class FireBaseUsersReponsitories //: IReponsitoriesFireBase<UserFireBase>
    {
        /* string collectionName = "users";
         RepositoriesFireBaseImpl<UserFireBase> repo;
         public FireBaseUsersReponsitories()
         {
             repo = new RepositoriesFireBaseImpl<UserFireBase>(collectionName);
         }

         public List<User> GetAll()
         {
             throw new NotImplementedException();
         }

         public UserFireBase Get(UserFireBase record)
         {
             throw new NotImplementedException();
         }

         List<UserFireBase> IReponsitoriesFireBase<UserFireBase>.GetAll()
         {
             throw new NotImplementedException();
         }

         public UserFireBase Add(UserFireBase record) => repo.Add(record);
         public bool Update(UserFireBase record) => repo.Update(record);
         public bool Delete(UserFireBase record) => repo.Delete(record);*/
    }
}
